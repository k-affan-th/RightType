[project]
name = "righttype-e2e"
version = "0.1.0"
description = "Windows E2E harness for RightType: launch, type, verify via UIA"
requires-python = ">=3.11"
dependencies = [
    "mss>=10",
    "pywinauto>=0.6.9",
]

[tool.uv]
package = false
